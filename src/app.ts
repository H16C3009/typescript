const userName = 'Max'
// userName = "as"

let age = 30

// var result
function add(a: number, b: number) {
    let result
    result = a + b
    return result
}

if (age >= 20) {
    let isAdult = true
}

console.log(isAdult)

if (age >= 20) {
    var isAdult = true
}

console.log(isAdult)
